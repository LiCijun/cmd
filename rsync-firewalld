#! /bin/bash
#set -e
if [ ! -n "$1" ] ;
then   
echo "请传入远程主机" ;
exit
fi
host=$1
echo 复制服务文件到远程服务器 $host
rsync -ahPvAX   /etc/firewalld/services/ 	$host:/etc/firewalld/services/
echo 
echo 远程服务器重载防火墙
ssh $host 'firewall-cmd --reload'




















